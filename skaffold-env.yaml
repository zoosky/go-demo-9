apiVersion: skaffold/v2alpha3
kind: Config
metadata:
  name: go-demo-9
build:
  local: {}
  artifacts:
  - docker: {}
    image: go-demo-9
  tagPolicy:
    envTemplate:
      template: "{{.IMAGE_NAME}}:{{.DEV_USER}}"
